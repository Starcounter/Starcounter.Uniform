namespace Starcounter.Uniform.Generic.FormItem
{
    /// <summary>
    /// Represents type of the message. Message can be either invalid ("true"), valid ("false") or neutral ("").
    /// </summary>
    public enum MessageType
    {
        Neutral,
        Valid,
        Invalid
    }
}
